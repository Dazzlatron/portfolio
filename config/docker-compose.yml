version: '3.8'
services:
  portfolio:
    build: 
      context: .
      dockerfile: config/Dockerfile
    ports:
      - "8080:80"  # Map host port 8080 to container port 80
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge