{
  "name": "portfolio",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npx http-server -p 8080 -a 0.0.0.0 --cors",
    "docker:dev": "docker-compose -f config/docker-compose.dev.yml up --build",
    "docker:prod": "docker-compose -f config/docker-compose.yml up --build",
    "docker:stop": "docker-compose -f config/docker-compose.dev.yml down && docker-compose -f config/docker-compose.yml down",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-import": "^16.0.0",
    "postcss-nested": "^6.0.1",
    "cssnano": "^6.0.3"
  },
  "dependencies": {
    "boxicons": "^2.1.4",
    "scrollmagic": "^2.0.8"
  }
}